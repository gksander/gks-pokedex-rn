import * as React from "react";
import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

export const PokeSearchView: React.FC = () => {
  return (
    <SafeAreaView>
      <Text>Poke search</Text>
    </SafeAreaView>
  );
};
